package com.eswap.common.constants;

public enum RecipientType {
    INDIVIDUAL,   // Gửi cho một cá nhân cụ thể (có userId)
    FOLLOWERS,        // Gửi cho một nhóm người dùng cụ thể
    ALL_USERS     // Gửi cho tất cả người dùng
}
